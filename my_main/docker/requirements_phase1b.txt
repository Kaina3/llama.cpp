# Phase 1B Python dependencies
# PyTorch is installed separately in Dockerfile (with CUDA wheels)

# Hugging Face ecosystem
# transformers 5.0+ is required for Gemma4 (gemma4_unified model type).
# transformers 5.x uses torch.float8_e8m0fnu at import time; the training
# script monkey-patches torch to provide a fallback on PyTorch < 2.7.
transformers>=5.0.0            # Gemma4UnifiedForConditionalGeneration support
tokenizers>=0.21.0
accelerate>=1.0.0
datasets>=3.0.0
huggingface_hub>=0.27.0

# LoRA / PEFT
peft>=0.14.0                   # LoraConfig with layers_to_transform, modules_to_save

# TRL (optional - not used in current script but useful for SFTTrainer experiments)
# trl>=0.13.0

# Quantization (4-bit QLoRA)
bitsandbytes>=0.43.0           # NF4 quantization + AdamW8bit optimizer

# Numerics / image / vision support
# Gemma4UnifiedProcessor unconditionally imports PIL and torchvision.transforms.v2
# (multimodal model; image path is always compiled even for audio-only training)
# torchvision must be installed via the same CUDA wheel index as torch.
numpy>=1.24.0
scipy>=1.10.0
pillow>=10.0.0
# torchvision: install with matching CUDA wheel, e.g.:
#   pip install torchvision --index-url https://download.pytorch.org/whl/cu126

# Safetensors for fast model loading
safetensors>=0.4.0
